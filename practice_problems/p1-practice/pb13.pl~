% 13. a. Transform a list in a set, in the order of the last occurrences of elements.
% Eg.: [1,2,3,1,2] is transformed in [3,1,2].
%  b. Define a predicate to determine the greatest common divisor of all
% numbers in a list.

% is_in_list(l1...ln,l) = { false, n = 0
%                           true, l1 = e
%                           is_in_list(l2...ln,l), otherwise
% is_in_list(i,i)
is_in_list([H|_],H):-!.
is_in_list([_|T],E):-
    is_in_list(T,E).

% transform(l1...ln) = { [], n = 0
%                      l1 U transform(l2...ln), is_in_list(l2...ln,l1)=false
%                      transform(l2...ln), is_in_list(l2...ln,l1)=true
% transform(i,o)
transform([],[]).
transform([H|T],[H|R]):-
    not(is_in_list(T,H)), !,
    transform(T,R).
transform([_|T],R):-
    transform(T,R).
