%8. a. Write a predicate to determine if a list has even numbers of
% elements without counting the elements from the list.
% b. Write a predicate to delete first occurrence of the minimum number
% from a list.

% even_list(l1...ln) = { true, list is empty at the end
%                        false, list has one elem left
%                        even_list(l3...ln),otherwise
% even_list(i)
even_list([]).
even_list([_,_|T]):-
    even_list(T).

% min(l1...ln) = { l1, n = 1
%                  min(l1,min(l2...ln)), otherwise
% min(i,o)
min([H],H).
min([H|T],MIN):-
    min(T,MIN2),
    ( H < MIN2 -> MIN=H; MIN=MIN2).

% delete_min(l1...ln,flag) = { [], n = 0
%                        l1 U delete_min(l2...ln,flag), l1!=min, flag=0
%                        delete_min(l2...ln,flag), l1 == min, flag=1
% delete_min(i,i,o)
delete_min([],_,[]).
delete_min([H|T],0,R):-
    min([H|T],MIN),
    H =:= MIN,!,
    delete_min(T,1,R).
delete_min([H|T],0,[H|R]):-
    delete_min(T,0,R).
delete_min([H|T],1,[H|R]):-
    delete_min(T,1,R).

